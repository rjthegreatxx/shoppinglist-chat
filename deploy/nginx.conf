# HTTP — serves acme.sh challenge, redirects everything else to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name chat.agnx.systems;

    root /var/www/chat.agnx.systems;

    location /.well-known/acme-challenge/ {
        root /var/www/chat.agnx.systems;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS — proxy to FastAPI
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name chat.agnx.systems;

    ssl_certificate /etc/letsencrypt/chat.agnx.systems/fullchain.cer;
    ssl_certificate_key /etc/letsencrypt/chat.agnx.systems/chat.agnx.systems.key;
    include /etc/nginx/snippets/ssl-params.conf;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;

        # Required for streaming responses
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 120s;
    }
}
